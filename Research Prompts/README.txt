ETHEREUM IN INDIAN INSTITUTIONAL FINANCE - RESEARCH PROMPTS
Drafted 31 August 2026.

Each module file below is ALREADY PREFIXED with the master block, so you can
paste any one of them straight into a deep research tool as a single message.
00-MASTER-BLOCK.txt and Z-reconciliation-pass.txt are standalone.

RUN ORDER
  1. A-what-actually-shipped.txt      (run first; C depends on it)
  2. B-india-perimeter.txt            (run second; E depends on it)
  3. C-where-the-value-is.txt         (the one that matters most)
  4. D-honest-comparison.txt
  5. E-objection-bank.txt             (after B and D)
  6. F-adoption-precedent.txt         (any time)
  7. Z-reconciliation-pass.txt        (last, with all six outputs in context)

Save each output as its own file next to these. The point is a queryable
evidence base, not a document.

RE-RUN Module B in late October 2026. India's regulatory perimeter moves
month to month; everything else has a longer shelf life.

TWO FAILURE MODES TO CHECK FOR IN THE OUTPUT
  - Confident figures with no primary source. If a number cannot be traced
    to a regulator, a filing, or a named data provider, it is unusable in an
    external document however often it appears online.
  - Quiet advocacy. If Module D comes back without a strong "where Ethereum
    is the wrong answer" section, or Module A without a substantial graveyard
    of failed initiatives, the run has failed. Re-run rather than accept it.
